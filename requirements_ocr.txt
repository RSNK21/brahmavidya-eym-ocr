# EyM OCR Server — Python Dependencies
# Install with: pip install -r requirements_ocr.txt

# Core web framework
flask>=2.3.0
flask-cors>=4.0.0

# Image processing (required)
opencv-python>=4.7.0
Pillow>=9.0.0
numpy>=1.21.0

# Neural HTR engine — Sanskrit Qwen2.5-VL and/or TrOCR-Devanagari (recommended;
# far more accurate on handwriting than Parinamika or Tesseract below).
# Install torch matching your platform/CUDA version — see https://pytorch.org/get-started/locally/
# for the exact command; the line below is a reasonable default for a CUDA 12.1 GPU box.
# For CPU-only (fine for the lighter TrOCR path, too slow for Qwen2.5-VL):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
transformers>=4.46.0
accelerate>=0.30.0
sentencepiece>=0.2.0
# Optional: 4-bit quantization for Qwen2.5-VL on smaller GPUs (set EYM_QWEN_4BIT=1)
# bitsandbytes>=0.43.0

# Parinamika model (TF 1.15 — requires Python 3.6-3.7 and CUDA if GPU)
# Legacy fallback; uncomment only if you have the Parinamika model downloaded in modelss/
# tensorflow==1.15.0
# tensorflow-estimator==1.15.1

# Tesseract Python wrapper (fallback OCR, requires system Tesseract install)
pytesseract>=0.3.10

# PDF support
PyMuPDF>=1.23.0     # Fast pure-Python PDF page rasterizer
# python-docx>=0.8.10

# Utilities
requests>=2.28.0
